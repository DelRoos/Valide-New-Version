{
  "name": "valide-firestore-rules-tests",
  "version": "1.1.0",
  "description": "Tests d'integration des regles Firestore directement sur le projet Firebase (sans emulateur). Cf. test/rules/README.md.",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "firebase": "^11.0.0",
    "firebase-admin": "^13.0.1"
  }
}
