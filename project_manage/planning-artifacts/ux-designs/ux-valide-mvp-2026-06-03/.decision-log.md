# Decision log — ux-valide-mvp

> Journal canonique des décisions UX prises pendant la création de `DESIGN.md` et `EXPERIENCE.md`. Lu automatiquement par les skills BMAD aval (`bmad-create-architecture`, `bmad-create-epics-and-stories`, `bmad-dev-story`).

---

## 2026-06-03 — Création initiale

**Skill** : `bmad-ux`
**Intent** : `Create`
**Mode** : Fast path (l'utilisateur a délégué autonomement « Lance le a » après le PRD ; sources d'identité visuelle déjà très complètes via le Design System HTML existant)
**Auteur** : Delano Roosvelt (interactive, langue FR)

**Inputs distillés :**

- [`../../specs/spec-valide-mvp/SPEC.md`](../../specs/spec-valide-mvp/SPEC.md) (capacités, contraintes, non-goals)
- [`../../prds/prd-valide-mvp-2026-06-03/prd.md`](../../prds/prd-valide-mvp-2026-06-03/prd.md) (44 FRs, 7 UJs, 15 NFRs cross-cutting)
- [`../../../../doc/tech/Valide - Design System.html`](../../../../doc/tech/Valide%20-%20Design%20System.html) (référencé en entrée par l'utilisateur — source unique de l'identité visuelle)
- [`../../../../doc/tech/Valide - Design.html`](../../../../doc/tech/Valide%20-%20Design.html) (maquettes par module M1-M18)

---

### Décisions de distillation prises pendant la rédaction

#### D1 — Le Design System HTML est la source unique de DESIGN.md, lifté littéralement

**Quoi** : Tous les tokens visuels de `DESIGN.md` (couleurs hex, polices, échelles, composants visuels) sont **extraits directement** des `:root { --primary: #2563EB; ... }` et des classes CSS du Design System HTML existant. Aucun token n'a été inventé.
**Pourquoi** : L'utilisateur a pointé explicitement ce fichier comme entrée. Il représente le travail de design déjà fait et validé en amont. Sally (skill `bmad-ux` en v6.8.0) doit **capturer**, pas **inventer**.
**Impact aval** : Quand l'équipe mobile implémentera les tokens en Dart (dans `core/theme/tokens.dart`), ils auront une correspondance 1:1 avec le HTML existant. Aucune divergence possible.

#### D2 — Échelle typographique étendue de 4 → 9 niveaux

**Quoi** : Le Design System HTML montre un Hero h1 (46 px), un h2 de section (30 px), un sub-head (13 px uppercase), et un type-scale interne. J'ai expliciter 9 niveaux nommés (`display`, `h1`, `h2`, `h3`, `body-strong`, `body`, `meta`, `caption`, `eyebrow`) en couvrant les usages typiques d'une app mobile.
**Pourquoi** : 4 niveaux ne suffisent pas pour les besoins de l'app (titre d'écran, titre de section, corps long, métadonnée, badge, etc.). L'extension est cohérente avec les graisses (400/600/700/800/900) du HTML.
**Risque** : Si le Design System HTML est mis à jour pour ajouter des niveaux différents, il faudra réconcilier. À tracer comme `[ASSUMPTION]` à valider visuellement.
**Mitigation** : Les noms sont sémantiques (`body-strong` plutôt que `text-16-700`) — restent valides quelle que soit la taille exacte.

#### D3 — Échelle d'espacement étendue à 10 valeurs (4, 8, 12, 16, 20, 24, 32, 40, 48, 64)

**Quoi** : Le Design System HTML annonce « grille 4 px » mais ne liste pas explicitement l'échelle. J'ai déclaré une progression standard 4 → 64 px.
**Pourquoi** : Échelle cohérente avec la grille 4 px annoncée et avec les valeurs déjà utilisées dans le HTML (16 / 24 / 32 / 64 px observés).
**Impact aval** : Sera implémenté dans `core/theme/tokens.dart` avec `flutter_screenutil` (les valeurs en `.h`, `.w`, `.sp`, `.r`).

#### D4 — 7 niveaux de rayons nommés au lieu de 3-4

**Quoi** : Le Design System HTML utilise des rayons à plusieurs valeurs (6, 9, 11, 14, 16, 18, 999 px observés). J'ai nommé 7 niveaux (`xs`, `sm`, `md`, `lg`, `xl`, `2xl`, `pill`).
**Pourquoi** : Cohérent avec ce qu'on observe dans le HTML. Permet d'avoir une grammaire claire (« lg pour boutons, 2xl pour cartes »).
**Règle ajoutée** : « ne pas mélanger 4 rayons différents sur le même écran » — privilégier `lg` + `2xl` comme paire dominante.

#### D5 — Bottom tab bar à 4 onglets (Accueil / Matières / Activités / Profil)

**Quoi** : Architecture top-level à 4 onglets dans une bottom tab bar. Pas de drawer hamburger.
**Pourquoi** : Cohérent avec les maquettes des modules M1-M18 (le Design HTML montre cette structure). Les 4 onglets couvrent les surfaces que l'élève visitera **quotidiennement** ; les sub-surfaces (santé scolaire, classements, chat, paywall) sont atteintes par drill-down ou actions contextuelles.
**Alternative écartée** : 5 onglets (séparer Activités et Examens). Le Design HTML montre que les examens sont une sous-section d'Activités, ce qui maintient une charge cognitive moindre.
**Impact aval** : Sally (UX) confirme cette IA dans le routing `go_router`.

#### D6 — Étiquettes santé scolaire `solide / à renforcer / priorité` (et non `bon / moyen / faible`)

**Quoi** : Lexique positivement orienté pour les trois étiquettes de santé.
**Pourquoi** : Cohérent avec le principe « Motivation » du Design System HTML (« encourager sans culpabiliser »). « Priorité » suggère une action, « Faible » suggère un échec. Cohérent aussi avec le Decoupage MVP qui utilise déjà ces termes.
**Impact aval** : Couvre toutes les surfaces (santé scolaire, recommandations, notifications). Verrouillé par le glossaire SPEC.

#### D7 — Couleur seule n'est jamais un signal — règle stricte

**Quoi** : Tout état (étiquette santé, statut paiement, erreur réseau) est **toujours doublé** d'un texte explicite et optionnellement d'une icône.
**Pourquoi** : Accessibilité (élèves daltoniens, vision diminuée). Cohérent avec le principe « Accessibilité » du Design System HTML. Standard WCAG.
**Impact aval** : Aucun composant `badge` ou `chip` ne s'affiche sans un texte.

#### D8 — Tutoiement français par défaut, ton équivalent direct en anglais

**Quoi** : Microcopie en FR utilise le tutoiement (« Tu peux »), en EN utilise un ton direct informel.
**Pourquoi** : Cible ado/jeune adulte, contexte familier d'app de pratique. Le vouvoiement créerait une distance émotionnelle inadaptée.
**Impact aval** : Toutes les chaînes ARB FR/EN à rédiger dans ce registre.

#### D9 — Pas d'émoji dans l'interface (sauf célébrations mesurées)

**Quoi** : Bannissement des émojis décoratifs dans les écrans courants.
**Pourquoi** : Le Design System HTML mise sur la sobriété (« moderne, clair, motivant » sans surcharge). Trop d'émojis = ton infantilisant ou marketing. Les rares cas autorisés : récap de réussite avec mention, montée de niveau (et même là, mesurés : un trophée Lucide vaut mieux que 🎉🎉🎉).
**Impact aval** : Sally et l'équipe Content valident ce choix dans la rédaction des chaînes.

#### D10 — Pas de dark mode en V1 — `[ASSUMPTION]` à valider

**Quoi** : Light mode only en V1.
**Pourquoi** : Le Design System HTML ne propose pas de palette sombre. Hypothèse que la consultation se fait majoritairement en journée par les élèves (lecture sur cahier en parallèle, lumière naturelle ou ampoule). Ajout d'un dark mode est un effort non trivial sans bénéfice marché clair en V1.
**Risque** : Les élèves qui révisent tard le soir peuvent ressentir l'inconfort.
**Mitigation** : Mettre dark mode candidate V2 si retour utilisateur. Logger dans `[ASSUMPTION]`.
**Impact aval** : `core/theme/app_theme.dart` ne déclare qu'un ThemeData light.

#### D11 — Streaks visibles sur profil mais sans notification de rupture

**Quoi** : La métrique « streak » (jours consécutifs d'activité) est visible sur le profil mais n'est **pas** au cœur de la gamification, et **ne génère pas** de notification culpabilisante en cas de rupture.
**Pourquoi** : Anti-pattern Duolingo explicite. Aller dans cette direction démotive les élèves qui ratent un jour (souvent pour des raisons hors de leur contrôle — coupure courant, examen IRL).
**Impact aval** : `gamification` feature ne crédite pas de bonus « streak conservé » au-delà du raisonnable. Pas de push « ton streak va se briser ! ».
**Risque** : ce choix de design peut réduire l'engagement quotidien des élèves « game-driven ». À mesurer post-MVP.

#### D12 — Paywall plein écran **avant** chargement de l'exercice premium

**Quoi** : Pour un compte gratuit qui tape Mode 2 / Mode examen / Fiches / Chat ×20, l'app affiche le paywall **avant** de charger l'exercice ou la ressource.
**Pourquoi** : (1) Économie data — pas de lecture Firestore inutile pour un contenu non accessible. (2) Sécurité — moins d'opportunités de fuiter du contenu premium par modification de l'app (cf. NFR-9 et FR-23 du PRD).
**Impact aval** : Le routing `go_router` vérifie le statut premium **avant** de naviguer vers les écrans premium. Cohérent avec l'archi mobile § 17.4.

#### D13 — Skeleton plutôt que spinner pour les chargements > 500 ms perçus

**Quoi** : Sur les écrans qui chargent du contenu structuré (cours, liste de matières, dashboard), utiliser un skeleton qui mime la forme finale plutôt qu'un spinner abstrait.
**Pourquoi** : Réduit la perception de lenteur (étude Nielsen Norman Group). Donne au cerveau quelque chose à pré-parser. Cohérent avec NFR-2 (démarrage < 3 s) — l'écran semble « presque prêt » même quand les données ne sont pas là.
**Impact aval** : Sally et Amelia (dev) implémentent un widget `Skeleton` réutilisable dans `core/widgets/`.

#### D14 — Pas de carrousel d'écrans top-level

**Quoi** : La bottom tab bar n'autorise pas le swipe horizontal entre les 4 onglets.
**Pourquoi** : Anti-pattern documenté — les utilisateurs « tombent » accidentellement sur un autre onglet en scrollant verticalement. La navigation par tap est plus prévisible.
**Impact aval** : Implémenter avec `PageView` désactivé pour le swipe ou `IndexedStack` qui n'a pas ce comportement.

#### D15 — Aucun mock HTML rendu en V1 — les spines suffisent

**Quoi** : Pas de rendu de key-screen mocks dans `.working/` ou `mockups/` pour cette première passe. Les spines (DESIGN + EXPERIENCE) suffisent.
**Pourquoi** : Le Design HTML existant (`Valide - Design.html`) sert déjà de référence visuelle pour les 18 modules. Ré-générer des mocks HTML serait redondant.
**Impact aval** : Sally pourra générer des mocks ciblés (en Update mode) si une feature spécifique le justifie en aval (ex. paywall qui mériterait une exploration A/B). Pour le MVP, les spines + le Design HTML existant suffisent.

#### D16 — Quatre breakpoints téléphone, aucun support tablette

**Quoi** : Mobile-only, avec breakpoints à 360 / 393 / 412 / 480 dp.
**Pourquoi** : Cohérent avec le Constraint « Android-first » du SPEC et le NFR-1 du PRD. Tablette = scope V2/V3.
**Impact aval** : Aucun layout responsif desktop/tablette en V1. Si lancé sur tablette par accident, mono-colonne centrée largeur max 480 px.

#### D17 — Anti-pattern « lecture cours interrompue par paywall »

**Quoi** : La lecture des cours est **gratuite** intégralement. Le paywall n'apparaît **que** lors d'une tentative de Mode 2 / Mode examen / Fiches / Chat ×20.
**Pourquoi** : Les cours sont du contenu de référence — couper l'accès aux notions de base démolirait la confiance et la valeur perçue. La monétisation porte sur la **pratique** (Modes) et le **chronométrage** (examens), pas sur la lecture passive.
**Impact aval** : Aucun écran de lecture cours ne déclenche un paywall. Aligné avec le SPEC CAP-2 (consultation libre) vs CAP-3-4-5-6 (modes et premium).

---

## 2026-06-03 — Verdict de self-validate

### Pass 1 — Coherence (UX rubric)

| Check | Statut | Note |
|---|---|---|
| DESIGN.md frontmatter YAML valide (Google Labs spec) | ✅ | Sections `colors`, `typography`, `rounded`, `spacing`, `elevation`, `components` toutes présentes |
| DESIGN.md sections canoniques (Brand & Style, Colors, Typography, Layout & Spacing, Elevation, Shapes, Components, Do's and Don'ts) | ✅ | Toutes présentes dans l'ordre |
| EXPERIENCE.md sections obligatoires (Foundation, IA, Voice & Tone, Component Patterns, State Patterns, Interaction Primitives, Accessibility Floor, Key Flows) | ✅ | Toutes présentes |
| Cross-references DESIGN ↔ EXPERIENCE | ✅ | EXPERIENCE.md référence les tokens via `{colors.X}`, `{spacing.X}`, etc. (syntaxe Google Labs respectée) |
| EXPERIENCE.md ne duplique pas les specs visuelles | ✅ | Les composants y sont décrits côté **comportement** ; spec visuelle pointée vers `DESIGN.md.Components` |
| Key Flows ont un climax explicite | ✅ | Chaque flux a un beat « Climax » nommé |
| Personas nommées (pas « l'utilisateur ») | ✅ | Fatou Mballa et James Tanyi (héritées du PRD) |
| Inspiration & Anti-patterns nommés | ✅ | Section dédiée |
| Responsive & Platform spécifié | ✅ | Section dédiée (Android-first, 4 breakpoints, pas de tablette) |
| Toutes les FRs du PRD ont une trace UX | 🟡 | Les 44 FRs sont reflétées par 8 flows et 17 surfaces IA. Les FRs admin et de logging interne ne génèrent pas de flow user-facing — c'est attendu. |
| Pas de couleur seule comme signal | ✅ | Règle documentée 2× (DESIGN.md.Colors + EXPERIENCE.md.Accessibility Floor) |

### Pass 2 — Preservation (vs PRD + Design System HTML)

| Source claim | Atterrissage |
|---|---|
| Palette `#2563EB` / `#0F172A` / etc. (Design System HTML) | DESIGN.md frontmatter `colors` + section Colors |
| Nunito Sans + JetBrains Mono | DESIGN.md frontmatter `typography` + section Typography |
| Grille 4 px | DESIGN.md `spacing` + section Layout & Spacing |
| Ombres soft / mid (Design System HTML) | DESIGN.md `elevation` + section Elevation & Depth |
| Boutons 52 px / 14 px radius (Design System HTML) | DESIGN.md `components.button` + section Components > Boutons |
| Cartes 18 px radius + ombre soft (Design System HTML) | DESIGN.md `components.card` + section Components > Cartes |
| Principes Simplicité / Motivation / Clarté / Accessibilité (Design System HTML) | DESIGN.md.Brand & Style |
| 7 UJs du PRD (UJ-1 à UJ-7) | EXPERIENCE.md.Key Flows (Flow 1 à 8, étendus avec edge cases) |
| Étiquettes santé `solide / à renforcer / priorité` | EXPERIENCE.md.Voice and Tone + glossaire SPEC |
| FR-4 garde profil incomplet | EXPERIENCE.md.IA > Règles de navigation |
| FR-14 lecture offline | EXPERIENCE.md.State Patterns > Offline open + Flow 2 |
| FR-17 Mode 1 photo avec compression | EXPERIENCE.md.Component Patterns > Photo Mode 1 + Flow 3 |
| FR-22 déblocage premium automatique post-webhook | EXPERIENCE.md.State Patterns > Success paiement + Flow 4 |
| FR-26 idempotence | EXPERIENCE.md.State Patterns > Error idempotence + edge case Flow 3 |
| FR-31 reco équilibrée 1/5 solide | EXPERIENCE.md.Flow 6 (climax) + State Patterns > recommandations |
| FR-35 plafonds notifications | EXPERIENCE.md.State Patterns + Interaction Primitives (bannis : re-engagement) |
| FR-38 mode examen sauvegarde continue | EXPERIENCE.md.State Patterns > Examen sauvegarde + Flow 7 |
| FR-41 chat IA ne donne pas la solution | EXPERIENCE.md.Voice and Tone + Flow 8 (climax) |
| FR-43 deep links partage | EXPERIENCE.md.IA + Flow 7 (étape 10-12) |
| 15 NFRs cross-cutting du PRD | EXPERIENCE.md.Accessibility Floor (NFR-6, 7, 14) + Responsive & Platform (NFR-1, 2, 3, 4, 15) + State Patterns (NFR-7, 15) + Component Patterns (NFR-4) |

**Wrapper-only content drops** (logged) :

- Détails de pixel-perfect des composants (border-radius exacts à 0.5 px près pour les cas exotiques) → délégué à l'implémentation Flutter en `core/theme/`. Pas une décision UX mais une transcription.
- Les SVG des icônes principes du Design System HTML (ampoule, courbe, cible, œil) → non spécifiés ici, à reprendre depuis le HTML par les devs.

### Verdict global

**🟢 PASS sur Cohérence et Préservation.** Les deux spines (DESIGN + EXPERIENCE) sont consommables par les skills aval (`bmad-create-architecture` pour Winston, `bmad-create-epics-and-stories` pour John, et les stories de dev). Les tokens visuels sont **figés et alignés** sur le Design System HTML existant — zéro divergence introduite.

**Open Questions UX restantes** (à trancher en aval) :

- **OQ-UX-1** — Élève sans école renseignée : afficher invitation explicite « Lier mon école » sur le dashboard, ou rester silencieux ? (Hérité PRD OQ-6, repris ici dans EXPERIENCE.md > Flow 6 empty state).
- **OQ-UX-2** — Dark mode V1 ou V2 ? (Hérité D10 ici). Mesurer post-MVP avec retour utilisateur.
- **OQ-UX-3** — Notification « action a besoin d'une connexion » avec relance auto vs message simple ? (Flow 2 edge case).
- **OQ-UX-4** — Couleur de la mini-carte de rang : neutre toujours (`primary`) ou colorée selon le rang (top 3 doré, milieu bleu, bas amber) ? À tester en preuve d'écran.

**Phase-blocker pour `bmad-create-architecture`** : aucune. Winston peut commencer immédiatement.

**Recommandation immédiate** : enchaîner sur `/bmad-create-architecture` (Winston). Le PRD + ces deux spines + les trois docs d'archi existantes lui donneront tout le contexte pour produire `architecture.md` final + ADRs.

**Imports** : aucun mock HTML importé en V1 — le Design HTML existant tient lieu de référence (cf. D15). Le dossier `imports/` reste vide pour le moment ; il pourra accueillir des Figma exports ou des wireframes Excalidraw si l'équipe en produit en aval.

---

## 2026-06-04 — Update : Posture émotionnelle + Multisensoriel (motion / audio / haptic)

**Skill** : `bmad-ux` (intent `Update`)
**Mode** : Fast (utilisateur a tranché les 3 paramètres clés en amont)
**Auteur** : Delano Roosvelt

### Trigger

L'utilisateur a signalé après merge de PR 0.3 (AppLogger) que trois dimensions transverses manquaient dans le scope :

1. **Animations** — « tout doit être animé »
2. **Sons + vibrations** — « pense aussi car c'est très important »
3. **Émotion** — « les interfaces ou il y a de l'émotion on doit dégager de l'émotion »

### Décisions

#### D-UX-Update-1 — Posture motion : *micro-interactions partout, transitions sobres*

**Quoi** : Tout élément interactif réagit visuellement au tap ; chaque changement d'état est animé ; les transitions inter-écrans restent standard (`slide page` 200 ms). Cible 60 fps sur Android Go.
**Pourquoi** : Compromis entre l'ambition « tout est animé » et la contrainte non-négociable « téléphones modestes ». Hero animations cinématiques et parallaxes rejetées (perf). Lottie écarté en V1 (taille APK + perf).
**Comment** : Tokens motion ajoutés à DESIGN.md § Animations & motion (`fast` 120 / `standard` 200 / `emphasis` 300 / `celebration` 600 ms + easings standardOut/In/emphasized + stagger 50 ms). Package retenu : `flutter_animate` + Implicit widgets natifs.
**Impact aval** : Story 0.10 (tokens) reçoit AC5 « Motion tokens » ; Story 0.13 (atoms) reçoit anim `tap feedback` 120 ms sur boutons + `selection click` haptic ; Story 0.14 (feedback) inclut désormais les overlays célébration animés.

#### D-UX-Update-2 — Posture audio : *standard 8-12 sons, budget ≤ 500 KB*

**Quoi** : Catalogue 12 sons OGG embarqués (tap, success_soft/strong, error_soft/strong, complete, levelup, badge, payment_ok, notification, streak, chat_send). Total cible ≤ 500 KB. Aucun streaming.
**Pourquoi** : L'utilisateur a tranché « Standard 8-12 sons ≤ 500 KB » plutôt que « minimaliste » ou « pas de son ». Justification : ambiance riche, mais pas de coût data en runtime et budget APK maîtrisé.
**Coupures globales** : silencieux Android → muet ; Mode Examen → muet ; setting Profil « Sons activés » (par défaut ON).
**Impact aval** : Story 0.14 reçoit AC9 « AudioService » + AC10 « overlays célébration » ; assets déclarés en `pubspec.yaml`. Production des sons à planifier en story P0 séparée (placeholders OGG silence si nécessaire pour livrer Story 0.14 avant production audio).

#### D-UX-Update-3 — Posture haptic : *discret mais présent, API Flutter native*

**Quoi** : Catalogue `selection / light / medium / heavy / success(séquence) / error(séquence)` via `HapticFeedback.*`. Pas de dépendance externe pour le haptic.
**Pourquoi** : Renforce chaque confirmation positive/négative significative sans alourdir l'APK. API native = zéro perm supplémentaire au-delà de ce que Flutter déclare déjà.
**Coupures globales** : batterie < 15 % → muet ; Mode Examen → muet ; setting Profil « Vibrations activées » (par défaut ON). Le mode silencieux Android n'affecte PAS le haptic (vibration différente du son).
**Impact aval** : Story 0.14 reçoit AC8 « HapticService » avec gestion des coupures.

#### D-UX-Update-4 — Posture émotionnelle explicite, 7 moments cibles

**Quoi** : Nouvelle section EXPERIENCE.md § Emotional Posture qui mappe 7 moments (onboarding, lecture, bonne/mauvaise réponse, paywall, examen, santé faible) sur une émotion cible, un levier principal et un anti-pattern. Règles d'écriture émotionnelle (« Pas tout à fait » plutôt que « Faux »).
**Pourquoi** : L'utilisateur a explicité que l'émotion est un levier produit (rétention, persévérance, usage), pas un vernis. Les UJs existants (Fatou, James) le supposaient implicitement ; on rend la règle décidable.
**Impact aval** : guide la copy de toutes les stories E1-E6. Pas de nouveau code direct mais référence dans la définition de chaque story de feature ayant un écran utilisateur.

#### D-UX-Update-5 — Choreography par moment clé, source unique

**Quoi** : Table de choreography (anim + son + haptic + copy) pour ~17 moments clés dans EXPERIENCE.md § Multisensoriel. Source unique consultée par les implémentations.
**Pourquoi** : Évite que chaque story réinvente la combinaison ; garantit la cohérence émotionnelle globale.
**Impact aval** : Quand une story implémente un écran, elle consulte cette table pour appliquer la combinaison correcte. Les services `HapticService` et `AudioService` (Story 0.14) sont les points d'appel.

### Ce qui N'A PAS été touché

- Tokens visuels (couleurs, typo, spacing, radii, élévations) : inchangés.
- Voice and Tone, Microcopie d'état : inchangés (cohérents avec D-UX-Update-4).
- Flows (Flow 1-8) : inchangés (les flows existants restent valides, la couche multisensorielle s'y greffe via la table de choreography).
- Information Architecture, Accessibility Floor, Responsive & Platform : inchangés.

### Open Questions UX nouvelles

- **OQ-UX-5** — Production des 12 sons : qui produit ? Sound designer externe, packs libres CC0, ou IA ? (impact : timing P0 + qualité ressentie)
- **OQ-UX-6** — Détection du mode silencieux Android : trouver une lib stable ou fallback uniquement sur le setting utilisateur ? (impact : Story 0.14 AC9)
- **OQ-UX-7** — Le setting Profil « Sons / Vibrations » est-il accessible dès l'onboarding ou seulement plus tard dans Profil ? (UX : 1er-use)

---

## 2026-06-04 — Update 2 : scope cross-platform (Android + iOS, phone + tablette)

**Skill** : `bmad-ux` (intent `Update`)
**ADR de référence** : [ADR-011](../../architecture/adrs/ADR-011-cross-platform-v1-android-ios-tablet.md)
**Trigger** : Directive porteur produit (2026-06-04)
**Auteur** : Delano Roosvelt

### D-UX-Update-6 — Responsive 3 form factors (phone / phone-landscape / tablet)

**Quoi** : Refonte de EXPERIENCE.md § Responsive & Platform avec 3 breakpoints (phone < 600 dp, phone-landscape ou small tablet 600-840 dp, tablet ≥ 840 dp). Patterns tablette ajoutés (split-view lecture, master-detail listes, NavigationRail paysage tablette).
**Pourquoi** : Le porteur a tranché « téléphone et tablette » et « responsive natif ». 3 layouts cibles couvrent les form factors réalistes sans exploser le nombre de variantes.
**Impact aval** : Story 0.12 reçoit AC5 (helper `Responsive`) et AC6 (Hello adaptée 3 form factors). Tous les écrans E1-E6 doivent porter explicitement les 3 layouts dans leurs ACs.

### D-UX-Update-7 — Plateforme iOS ajoutée § Responsive & Platform

**Quoi** : Nouvelle section dans EXPERIENCE.md couvrant les spécificités iOS : VoiceOver (équivalent TalkBack), Dynamic Type, permissions `Info.plist`, swipe-from-edge back, Cupertino route autorisée si améliore UX, hardware ringer switch iOS et limites de détection silencieux.
**Pourquoi** : Sans cette section, les stories E1+ risquent d'oublier iOS et de produire un design Android-only.
**Impact aval** : guide les stories de feature à inclure les considérations iOS dans leurs ACs.

### D-UX-Update-8 — Coupures globales multisensorielles enrichies iOS

**Quoi** : Table § Multisensoriel > Coupures globales étendue avec :
- Mode silencieux iOS (pas d'API publique → fallback setting Profil utilisateur + AVAudioSession ambient optionnel).
- Low Power Mode iOS (`isLowPowerModeEnabled` via plugin).
- `MediaQuery.disableAnimations` clarifié comme couvrant aussi iOS Reduce Motion.
**Pourquoi** : Sans cette enrichissement, le `HapticService` et `AudioService` de Story 0.14 ignoreraient les comportements iOS attendus.
**Impact aval** : Story 0.14 AC8-AC9 doivent gérer ces conditions cross-platform.

### D-UX-Update-9 — Audio passe OGG → AAC/M4A

**Quoi** : DESIGN.md § Audio : extension `.ogg` remplacée par `.m4a` dans le catalogue. Format AAC mono 22 kHz ~32-64 kbps.
**Pourquoi** : OGG **non supporté nativement iOS** ; conversion runtime serait coûteuse en perf. AAC/M4A supporté nativement Android + iOS sans transcoding.
**Impact aval** : Production des 12 sons cible AAC/M4A. Si on a déjà des OGG, conversion nécessaire (préserver qualité, contrôler taille — sf-convert ou ffmpeg).

### D-UX-Update-10 — Mapping haptic Android vibrator / iOS Taptic documenté

**Quoi** : Catalogue § Haptics enrichi avec colonnes « Mapping iOS (Taptic) » et « Mapping Android (vibrator) » pour chaque preset. Note assumant la divergence d'intensité ressentie (Taptic plus précis que vibrator linéaire).
**Pourquoi** : Les devs implémentant Story 0.14 ont une référence claire ; les designers reviewing comprennent les limites cross-platform.
**Impact aval** : Story 0.14 AC8 — les tests vérifient que la méthode `HapticService` appelée déclenche bien le `HapticFeedback.*` correspondant ; on n'attend PAS de tests d'intensité identique sur les 2 OS.

### D-UX-Update-11 — Layout tablette dans DESIGN.md § Layout & Spacing

**Quoi** : Nouvelle § Layout tablette (≥ 840 dp) :
- Largeur de lecture max 600 dp centrée.
- Grilles 5-6 colonnes matières.
- Marges 24-32 px tablette (au lieu de 16 phone).
- Bottom tabs portrait tablette / NavigationRail paysage tablette.
- Split-view cours+sommaire autorisé paysage tablette uniquement.
- Pas de hero étalé sur tablette (max 600 dp centré).
**Pourquoi** : Cadre les patterns tablette une fois pour toutes plutôt que de laisser chaque story le réinventer.
**Impact aval** : Stories de feature E1-E6 consultent cette section pour décider de leurs adaptations tablette.

### Ce qui N'A PAS été touché par cet update

- Couleurs, typo, spacing tokens (inchangés).
- Voice and Tone, Microcopie (la posture émotionnelle est universelle Android/iOS).
- Posture émotionnelle (univers commun).
- Catalogues d'animations (motion tokens inchangés, juste le packaging d'assets audio).
- Flows utilisateur (Flow 1-8 restent valides ; la couche multisensorielle s'y greffe via la table de choreography déjà documentée).

### Open Questions UX nouvelles

- **OQ-UX-8** — Hardware ringer switch iOS : configurer `AVAudioSession` category `ambient` ou rester sur setting Profil seulement ? Impact perçu vs effort. À trancher Story 0.14.
- **OQ-UX-9** — Cupertino back swipe iOS : activer par défaut ou stricte navigation Material ? Décision selon retours testflight. À trancher Story 0.13 ou plus tard.
- **OQ-UX-10** — Patterns tablette spécifiques à E1-E6 : la section § Layout tablette est cadre, mais chaque story doit décider de ses propres adaptations. Risque de fragmentation si on ne maintient pas un fil rouge.

---

## 2026-06-11 — Update 3 : refonte intégrale du flow pré-dashboard (templates `doc/templates/`)

**Skill** : `bmad-ux` (intent `Update`)
**Mode** : Fast path (l'utilisateur a tranché les 3 décisions structurantes en amont via questionnaire)
**Auteur** : Delano Roosvelt
**Templates de référence** : `doc/templates/src/components/OnboardingFlow.tsx` (791 l) + `doc/templates/src/data/educationData.ts` + `doc/templates/src/types.ts`

### Trigger

L'utilisateur a signalé que les interfaces et le workflow d'onboarding existants ne donnent pas le résultat voulu. Il a fourni un prototype React/TS (10 étapes) comme **source de vérité du flow** et demande une refonte intégrale du parcours pré-dashboard. Le dashboard est explicitement exclu du scope. Périmètre confirmé : Splash + AccountCreation/visiteur + Onboarding (10 étapes) + Identité + École + Success + Profil post-onboarding (édition).

### Décisions structurantes

#### D-UX-Update-12 — Le template `OnboardingFlow.tsx` devient la source de vérité du flow d'onboarding (le DESIGN HTML reste celui de l'identité visuelle)

**Quoi** : Le flow d'onboarding (séquencement des écrans, conditionnalité, contenu de chaque étape) est désormais piloté par `doc/templates/src/components/OnboardingFlow.tsx`. Le Design HTML existant (`Valide - Design.html`) reste l'autorité visuelle des écrans (palette, typo, composants). Les tokens (`tokens.dart`) sont déjà parfaitement alignés avec les couleurs du template (`#2563EB`, `#16A34A`, etc.) — zéro divergence à introduire.

**Pourquoi** : L'utilisateur a pointé explicitement ce prototype comme la cible. La refonte précédente d'Epic 1 (Stories 1.1-1.17) a produit un flow fonctionnel mais qui ne donne pas le ressenti voulu sur les ratios texte / hiérarchie / espacement. Le prototype React montre concrètement la structure visée : Hero step illustré, picker sans wall-of-text, footer CTA en gradient remontant, transitions popLayout.

**Impact aval** : Toutes les stories E1 sont à reconsidérer. Création d'un nouvel Epic « E1bis — Refonte flow pré-dashboard » à découper via `/bmad-create-story` après finalisation de ce DESIGN.md/EXPERIENCE.md.

#### D-UX-Update-13 — Le flow d'onboarding passe à **10 étapes** consolidées (vs 7 actuelles + 4 variants picker)

**Quoi** : Nouvelle structure linéaire à 10 étapes (indexées 0 à 9) :

| # | Étape | Contenu | Conditionnel |
|---|---|---|---|
| 0 | **Sub-system** | Choix Francophone / Anglophone | — |
| 1 | **Hero intro** | Illustration + 3 features (Cours / Exercices / Chat IA) | NOUVELLE étape (n'existait pas) |
| 2 | **Track** | Général / Technique | — |
| 3 | **Level** | Liste niveaux selon (subSystem, track) | — |
| 4 | **Stream/Subjects** | Cards séries OU picker matières (lockedCore + electives + counter) | seulement si `level.requiresOrientation` |
| 5 | **Auth** | Google / Apple / Visiteur (3 boutons) | — (inversion vs Epic 1) |
| 6 | **Name** | Prénom + Nom | sauf si auth Google/Apple a fourni le nom |
| 7 | **Phone** | +237 + 9 chiffres | — (nouvelle étape) |
| 8 | **School** | Recherche + suggestions + "Ajouter mon école" + "Passer pour l'instant" | optionnel avec micro-friction |
| 9 | **Success** | Célébration confetti + CTA "Entrer dans mon espace" | — |

**Consolidation des variants** : les 4 variants picker (Aïssatou Tle franco générale 12 cards / Mariam O-Level panier / James A-Level extension / Eyong TVEE) sont **consolidés en step 4** avec rendu conditionnel selon `pickerMode` issu de `derivation_rules` :
- `derived` → écran récap (no pick)
- `series_only` (FR ≥ 2nde Gén & FR Tech) → cards séries
- `free_with_obligatory` (EN Gén Form 5/Lower Sixth) → 3 lockedCore + electives + counter ∈ [6, 11]
- `series_plus_optional` (EN Gén Upper Sixth S1-S8) → 3 series lockées + 4 transversales + counter ∈ [3, 5]
- `tve_picker` (EN Tech) → 3 Pro + 3 Related + Other electives + counter ∈ [6, 8]

**Pourquoi** : Le template introduit explicitement le Hero step (réduit le taux d'abandon en posant la valeur produit avant la friction du picker) et l'inversion Auth/Identité (déclenche l'engagement utilisateur en faisant percevoir la valeur d'abord). La consolidation step 4 simplifie la navigation : une seule URL canonique `/onboarding/stream-subjects` qui rend la bonne UI selon le profil, au lieu de 5 routes distinctes.

**Impact aval** : Réécriture totale de `lib/features/onboarding/presentation/` (au moins 8 pages cibles : `splash`, `sub_system_choice`, `hero_intro`, `track_choice`, `level_choice`, `stream_subjects_picker`, `auth_choice`, `name_input`, `phone_input`, `school_search`, `success_celebration`). La state machine Riverpod (`OnboardingNotifier`) doit aussi être refondue avec un step machine déterministe (currentStep + transitions next/back).

#### D-UX-Update-14 — Inversion Auth ↔ Onboarding : Auth déclenchée au step 5 (après le picker)

**Quoi** : L'authentification Firebase n'a lieu QUE au step 5, après que l'utilisateur a renseigné sub-system, track, level et stream/subjects. Avant le step 5, le profil partiel vit en mémoire (Riverpod `OnboardingDraftNotifier`) **uniquement** — aucune écriture Firestore. Au step 5, l'utilisateur tape l'un de 3 boutons :
- **Google** → flow Firebase Google Sign-In → `auth_provider: 'google'` → nom auto-rempli depuis `displayName` → skip step 6 si nom présent → continue step 7 (phone).
- **Apple** → flow Firebase Sign in with Apple (iOS uniquement, désactivé Android) → idem.
- **Visiteur** → Firebase anonymous auth → `auth_provider: 'guest'` → skip steps 6-8 → direct step 9 success simplifié.

Une fois l'auth réussie, le profil partiel (steps 0-4) est immédiatement écrit dans `users/{uid}` via `WriteBatch`. Si l'auth échoue (réseau coupé, refus utilisateur), l'utilisateur reste au step 5 avec toast d'erreur explicite (cf. CLAUDE.md règle 13 : message localisé par `failure.kind`).

**Pourquoi** : Hypothèse produit confirmée par utilisateur — l'élève montre une intention plus forte (engagement) après avoir investi 30 secondes à choisir sa série/matières qu'avant. Le flow Auth-first d'Epic 1 cassait sur les écrans 1-3 trop tôt (avant que la valeur soit perçue).

**Risque** : si l'utilisateur kill l'app entre steps 0 et 4, son profil partiel est **perdu** (Riverpod en mémoire). Trade-off accepté en V1 — la persistance partielle vers SharedPreferences sera ajoutée en V2 si la métrique d'abandon le justifie.

**Impact aval** :
- Story de migration : refonte de `lib/features/auth/` et `lib/features/onboarding/` pour basculer du mode Auth-first vers Onboarding-first + auth tardive.
- Suppression des routes intermédiaires `/onboarding/sub-system`, `/onboarding/track`, etc. à la racine ; consolidation sous `/onboarding/{step-name}`.
- `firestore.rules` : tolérer la création de `users/{uid}` après auth uniquement (déjà le cas), mais accepter un payload initial complet (subSystem + track + level + streamId + pickedSubjects en un seul `set()`).

#### D-UX-Update-15 — `examTarget` dérivé silencieusement, plus jamais affiché en sélection UI

**Quoi** : L'examen visé (BAC, BEPC, Probatoire, GCE O-Level, GCE A-Level, TVE IL, TVE AL) est **dérivé** côté Cloud Function `deriveProfile` à partir du tuple `(subSystem, track, level, streamId)`. Aucune étape UI ne demande à l'utilisateur de choisir son examen visé. Il apparaît uniquement en lecture sur :
- Le step 9 success page (« Ton espace BAC D est prêt »).
- Le bandeau dashboard (hors scope).
- Le profil consultation.

**Pourquoi** : L'examen visé est déterministe pour 99% des cas (un Tle D francophone prépare le BAC D, pas le BEPC). Demander à l'utilisateur introduisait une 8ème étape sans valeur ajoutée + un risque d'incohérence (élève qui aurait choisi BEPC en Tle D, bug invalidant les recos). Aligné avec ADR-016 catalogue v2 et la logique Story 1.6 déjà implémentée.

**Impact aval** : aucune nouvelle dépendance ; le mapping `(level, streamId) → examTargetId` existe déjà via `derivation_rules`. Surface UI à supprimer si elle existait : aucune (Epic 1 avait déjà cette dérivation silencieuse).

#### D-UX-Update-16 — Étape téléphone (+237) réintroduite au step 7 avec masquage logs strict

**Quoi** : Nouvelle étape 7 dédiée au numéro de téléphone camerounais. UI = drapeau Cameroun + indicatif `+237` figé + input numérique à 9 chiffres avec mask `6 XX XX XX XX`. Validation client : `^[26][0-9]{8}$` (préfixes opérateurs camerounais 6 et 2). Stockage Firestore : champ `phoneNumber: string` au format E.164 (`+2376XXXXXXXX`).

**Masquage logs OBLIGATOIRE** : aucun log AppLogger ne reçoit le numéro complet. Toujours masquer en `+237 XX XX XX X7 89` (4 derniers visibles) via helper `maskPhone(String): String` dans `core/logging/log_safe.dart` (à créer si manquant). Aligné CLAUDE.md § Sécurité règle 4 (« ne jamais logger numéros de téléphone complets »).

**Pourquoi** : Le téléphone est utilisé pour (a) récupération de compte si perte du provider OAuth, (b) liaison future avec paiement MoMo Freemopay (ADR-013) — qui demande le MSISDN. Le récupérer à l'onboarding évite une demande supplémentaire au premier paiement.

**Optionnel ou obligatoire ?** Présomption : **optionnel** (skip autorisé avec micro-friction si l'utilisateur tape « Continuer » sans saisir). Justification : ne pas bloquer un élève en zone réseau dégradée qui n'a pas son téléphone à portée. Le toast micro-friction dit « Tu pourras l'ajouter plus tard depuis ton profil » + advance step 8.

**Impact aval** :
- Nouveau composant `PhoneInputWithCountryFlag` à ajouter au catalogue COMPOSANTS-REUTILISABLES.md.
- Helper `maskPhone()` dans `core/logging/log_safe.dart`.
- Champ Firestore `phoneNumber: string?` ajouté à `users/{uid}` (à documenter dans `doc/partage/BASE-DE-DONNEES.md` lors de la story d'implémentation, avec accord backend).

#### D-UX-Update-17 — École : skip autorisé avec micro-friction (pas obligatoire)

**Quoi** : Le step 8 « École » permet à l'utilisateur de passer sans renseigner. Trois chemins UI :
1. **Trouver son école** : tape dans le champ recherche → suggestions filtrées (Firestore `schools` collection avec `arrayContains` sur `name_tokens` + `limit(10)`) → tap sur un résultat → bouton primaire « C'est mon école ».
2. **Ajouter une école absente** : si la recherche ne renvoie rien, bouton secondaire pointillé « + Ajouter "<saisie>" » → écriture `school_requests/{id}` (collection séparée modérée par admin pédagogique) → toast « Ton école est ajoutée, on la valide bientôt » → avance step 9.
3. **Passer pour l'instant** : bouton tertiaire en bas → toast micro-friction « Tu pourras l'ajouter plus tard depuis ton profil pour rejoindre les classements de ta classe » + avance step 9. `schoolId: null` en Firestore.

**Pourquoi** : Confirmé par l'utilisateur (questionnaire). Hypothèse produit : ne pas perdre un élève en onboarding parce qu'il ne trouve pas son école dans la liste seed limitée V1. Le micro-toast nudge l'utilisateur sans le bloquer.

**Risque** : adoption de la fonctionnalité classement de classe ralentie tant que les écoles ne sont pas renseignées. Mitigation : la mini-carte de rang en dashboard (hors scope ici) affichera un nudge « Lie ton école » pour les profils sans `schoolId`.

**Impact aval** :
- Nouveau composant `SchoolSearchWithAdd` à ajouter au catalogue.
- Collection Firestore `school_requests` (nouveau) — à documenter dans `doc/partage/BASE-DE-DONNEES.md`.
- Cloud Function de modération admin (out of mobile scope) — référence dans `doc/partage/CONTRATS-API.md`.

#### D-UX-Update-18 — Mode visiteur : saute steps 6-8, success simplifié, profil scolaire conservé

**Quoi** : Le bouton « Continuer comme visiteur » au step 5 déclenche `Firebase signInAnonymously()` puis :
1. Écrit le profil partiel (steps 0-4) en Firestore `users/{anonUid}` avec `isAnonymous: true`.
2. **Saute** les steps 6 (name), 7 (phone), 8 (school) — `displayName: null`, `phoneNumber: null`, `schoolId: null`.
3. Navigue direct au step 9 **success simplifié** : copy alternatif « Tu peux explorer librement. Crée ton compte plus tard pour sauvegarder ta progression. » + CTA « Explorer ».
4. Le dashboard reçoit (hors scope) un bandeau permanent « Visiteur — crée ton compte pour ne rien perdre » avec deep link vers `/auth/upgrade`.

**Pourquoi** : Confirmé par l'utilisateur (questionnaire). Justification : permettre une exploration low-friction (ado curieux qui découvre l'app dans un cyber, n'a pas envie de créer un compte tout de suite) tout en conservant son profil scolaire pour personnaliser le dashboard de découverte. Le profil scolaire (subSystem, track, level, streamId, subjects) est **toujours** sauvegardé en Firestore — pas en SharedPreferences local. Justification : permet une migration ultérieure smooth quand l'utilisateur passe de visiteur à compte (link anonymous → permanent).

**Risque** : un visiteur peut accumuler une progression (santé scolaire, points) qu'il perd s'il ne fait jamais l'upgrade. Mitigation : bandeau persistant + check à 7 jours qui propose explicitement la conversion.

**Impact aval** :
- Logique de `linkWithCredential` à documenter pour la migration visiteur → compte (Story dédiée Epic upgrade — hors scope ce passage).
- Firebase Auth doit avoir Anonymous Authentication activé dans le projet `valide-edu` (à vérifier — n'est pas le défaut).

#### D-UX-Update-19 — Identifiers code uniformément anglais — alignement template + glossaire CLAUDE.md

**Quoi** : Tous les noms de variables, classes, fichiers, attributs Firestore, routes, clés ARB Dart utilisés dans les spines et stories descendantes utilisent l'anglais selon glossaire CLAUDE.md règle Git §5 :

| Français (template ou Epic 1) | Anglais (cible) |
|---|---|
| `filiere` / `filière` / `Filiere` | `track` |
| `niveau` / `Niveau` | `level` |
| `serie` / `série` / `Serie` / `serieId` | `stream` / `streamId` |
| `matiere` / `matière` / `Matiere` | `subject` |
| `sous-systeme` / `subSysteme` | `subSystem` (déjà appliqué) |
| `examen visé` / `examenVise` | `examTarget` (déjà appliqué) |
| `école` / `ecole` / `Ecole` | `school` (déjà appliqué) |

**Pourquoi** : Le template React/TS utilise `serieId` (français) — c'est une dette à NE PAS reproduire en Flutter. CLAUDE.md règle 5 du workflow Git est explicite : tout nouveau code post-2026-06-10 est en anglais sans exception. La dette historique Epic 1 (`filiereId`, `niveauId`) sera résorbée par Story 1.19 dédiée (rename massif coordonné).

**Exceptions** :
- Commentaires et docstrings Dart restent en français (CLAUDE.md règle implicite : doc = FR).
- Valeurs string ARB FR/EN restent dans la langue cible.
- IDs Firestore (clés de documents, ex. `francophone_terminale_d`) peuvent rester en langue (data lexicale, pas du code).

**Impact aval** : les stories E1bis utilisent strictement `streamId`, `trackId`, `levelId`, `subSystem`, `examTarget`, `schoolId`, `subjectIds`, `pickedSubjects` dans tous les noms de classes/variables/champs Firestore.

#### D-UX-Update-20 — 4 nouveaux composants ajoutés au catalogue (PR refonte E1bis)

**Quoi** : 4 nouveaux widgets réutilisables à créer dans `lib/core/widgets/` durant la refonte et à documenter dans `doc/tech/COMPOSANTS-REUTILISABLES.md` (CLAUDE.md règle 11) :

1. **`SubSystemHeroCard`** (`lib/core/widgets/onboarding/sub_system_hero_card.dart`)
   Carte de sélection sub-system (Francophone / Anglophone) plein largeur, padding généreux, icône optionnelle, state selected (ring 2px primary + bg primary-soft + scale 1.01). Pas pour les autres écrans — les pickers track/level utilisent `SelectionCard` générique (ci-dessous).

2. **`SelectionCard` (générique)** (`lib/core/widgets/cards/selection_card.dart`)
   Carte sélectionnable réutilisable : icon optionnel + titre + desc optionnel + indicateur radio à droite. Variants compact / standard. Utilisée steps 0, 2, 3, 4 (cards séries). Pas un nouveau composant si `LevelCard` existant peut absorber via paramètres — à vérifier en story.

3. **`PhoneInputWithCountryFlag`** (`lib/core/widgets/forms/phone_input_with_country_flag.dart`)
   Input téléphone avec SVG drapeau Cameroun figé à gauche + indicatif `+237` non éditable + champ numérique mask `6 XX XX XX XX`. Validation regex `^[26][0-9]{8}$`. Helper `maskedForLogs` getter qui retourne le format masqué pour AppLogger.

4. **`SchoolSearchWithAdd`** (`lib/core/widgets/forms/school_search_with_add.dart`)
   Champ de recherche école + suggestions live (Firestore `arrayContains` sur `name_tokens` + `limit(10)`) + bouton « + Ajouter "<saisie>" » apparaissant si zéro résultat. Tap sur résultat → propagation `(schoolId, schoolName)` à parent. Tap sur « Ajouter » → callback `onAddRequest(String name)`.

5. **`CelebrationConfettiSuccess`** (`lib/core/widgets/feedback/celebration_confetti_success.dart`)
   Écran plein largeur célébration : checkmark animé central (spring scale 0→1 délai 100ms) + 3 micro-icônes orbitantes (PartyPopper top-left, Sparkles bottom-right, CheckCircle right) + canvas confetti (package `confetti` ou implémentation manuelle CustomPaint si trop lourd) + titre + sous-titre + CTA full-width. Couleurs : success-soft halo, success ink. Auto-dispatch onComplete après 3.5s OU au tap CTA. Coupures globales : si `MediaQuery.disableAnimations` → pas de confetti, juste un checkmark statique + fade-in 200ms.

**Pourquoi** : Le template emploie ces patterns spécifiques qui n'ont pas d'équivalent dans le catalogue actuel. CLAUDE.md règle 11 oblige la documentation simultanée.

**Impact aval** : 5 entrées catalogue + 5 fichiers Dart à créer + tests goldens par composant + au moins 1 test tablet (>840 dp) par composant. La story de refonte E1bis doit inclure ces ajouts comme dépendances structurelles avant les pages.

### Réconciliation avec décisions antérieures

| Décision antérieure | Statut | Note |
|---|---|---|
| D5 — Bottom tab bar 4 onglets | ✅ Inchangée | Dashboard hors scope |
| D9 — Pas d'émoji décoratif | ⚠️ Exception step 9 célébration | Le canvas confetti et les 3 icônes orbitantes restent des **icônes Lucide**, pas des emojis. Aligné. |
| D11 — Streaks sans notification rupture | ✅ Inchangée | — |
| D14 — Pas de carrousel top-level | ✅ Cohérent | Le template utilise transitions popLayout par bouton CTA, pas de swipe horizontal. |
| D16 — Mobile-only 4 breakpoints téléphone | ❌ Déjà remplacée par D-UX-Update-6 (3 form factors + tablette) | Rappel pour les implémenteurs. |
| D-UX-Update-1/2/3/4/5 (multisensoriel) | ✅ Inchangées | La table de choreography reçoit 1 entrée nouvelle : **Step 9 célébration onboarding** = anim `celebration` 600ms + son `complete.m4a` + haptic `success` + copy « Tout est prêt, <prénom>. ». |
| D-UX-Update-6/7/8/9/10/11 (cross-platform) | ✅ Inchangées | Les 10 écrans onboarding cibles **doivent** porter explicitement le layout 3 form factors (phone < 600, phone-landscape 600-840, tablet ≥ 840) — non négociable cf. CLAUDE.md règle 5. |

### Ce qui N'EST PAS touché par cet update

- **Tokens visuels** (couleurs, typo, spacing, radii, élévations, motion, audio, haptic) : strictement inchangés. Le template utilise les mêmes hex que tokens.dart.
- **Flows 2-8** (lecture cours, photo Mode 1, paywall, Mode 2, santé scolaire, examen, chat IA) : strictement inchangés. La refonte ne concerne QUE les flows pré-dashboard.
- **IA top-level (Dashboard, Matières, Activités, Profil)** : strictement inchangée (4 onglets confirmés).
- **Posture émotionnelle, Multisensoriel choreography (sauf 1 ajout step 9)** : inchangés.
- **Accessibility Floor, Responsive & Platform (frames)** : inchangés (rappel : enforcer les patterns existants sur les nouveaux écrans).
- **Voice & Tone microcopie d'état** : enrichi mais structure conservée. Toutes les nouvelles copies (steps 0-9 FR + EN) sont ajoutées en bas du tableau microcopie d'état + section dédiée « Microcopie onboarding (10 étapes) » dans EXPERIENCE.md.

### Open Questions UX nouvelles (à trancher en aval)

- **OQ-UX-11** — Step 1 Hero illustration : on garde l'asset PNG du template (`onboarding_hero_1781128407337.png`) ou on commande une illustration propre Valide School ? Impact : qualité ressentie + droits d'usage. Présomption : asset propre commandé (graphiste freelance, brief « élève camerounais étudiant le soir, table chaleureuse, livres ouverts, style illustration douce ») — à trancher Story illustration E1bis.
- **OQ-UX-12** — Step 5 Apple Sign-In sur Android : masquer le bouton complètement, ou afficher mais désactivé avec tooltip « Disponible sur iPhone » ? Présomption : masquer (cohérent template + UX moins confuse) — à confirmer Story 5 auth.
- **OQ-UX-13** — Step 4 picker matières EN Général : compteur live « 8/11 » affiché en sticky top OU en sticky bottom près du CTA Valider ? Présomption : sticky top (cohérent template) — à valider au goldenstep tablet.
- **OQ-UX-14** — Mode visiteur upgrade : à quel moment exact propose-t-on la conversion « créer un compte » (X jours d'usage, après X actions, après santé scolaire visible, ...) ? Hors scope onboarding mais lien direct — à trancher Story Epic upgrade.
- **OQ-UX-15** — Hero step 1 : skip autorisé via gesture (long-press sur progress, swipe rapide) ou strictement bouton « Commencer » ? Présomption : strictement bouton (cohérent avec D14 pas de carrousel) — à valider goldenstep.

### Phase-blocker pour `bmad-create-story`

Aucun. Une fois ce log + DESIGN.md + EXPERIENCE.md actualisés et marqués `final`, la skill `/bmad-create-story` peut être lancée pour découper en stories de migration Flutter (estimation : 5-8 stories couvrant infra state machine + 10 pages + composants + tests goldens + migration Firestore schema + dépréciation routes Epic 1).

### Recommandation immédiate

Enchaîner sur `/bmad-prd` (intent `Update`) **avant** `/bmad-create-story`. Justification : le PRD doit être mis à jour pour acter l'inversion Auth/Onboarding (impacte FR-1, FR-2, FR-8 minimum), l'ajout du step Phone (FR-NEW), et la confirmation du mode visiteur scope (FR-3). Sans cet alignement PRD, les stories E1bis manqueront de référentiel FR stable.
